import React from 'react'
import { SlideItem } from '../Data/HomeSlide'



export const Home = () => {
  return (
    <div>
        {SlideItem.map((e) => 
        <image />
        
        )}
    </div>
  )
}
